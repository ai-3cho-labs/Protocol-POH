# Protocol Frontend - Cloudflare Pages Configuration
name = "protocol-frontend"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Pages configuration
pages_build_output_dir = ".vercel/output/static"

[vars]
# Public environment variables (set in Cloudflare Dashboard for secrets)
# NEXT_PUBLIC_API_URL = "https://protocol-api.koyeb.app"
# NEXT_PUBLIC_WS_URL = "wss://protocol-api.koyeb.app/ws"
