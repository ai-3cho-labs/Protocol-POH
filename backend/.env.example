# ===========================================
# POH + GOLD Dual-Token Backend Environment Variables
# ===========================================
# POH = Token users hold (determines eligibility & hash power)
# GOLD = Token distributed as rewards
# Copy this file to .env and fill in your values

# Environment (development | production)
ENVIRONMENT=development
DEBUG=false
TEST_MODE=false

# Solana Network (mainnet-beta | devnet)
SOLANA_NETWORK=mainnet-beta

# API
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000

# ===========================================
# API Authentication
# ===========================================
# Comma-separated list of valid API keys
# Leave empty to allow unauthenticated access (development only)
API_KEYS=
API_KEY_HEADER_NAME=X-API-Key

# Admin API key for /api/admin/* endpoints (required)
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=

# ===========================================
# Database (Neon PostgreSQL)
# ===========================================
# Get from: https://neon.tech
DATABASE_URL=postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/protocol?sslmode=require

# ===========================================
# Redis (Upstash)
# ===========================================
# Get from: https://upstash.com
# Use rediss:// (with TLS) for production!
REDIS_URL=rediss://default:xxxxx@xxxxx.upstash.io:6379
UPSTASH_REDIS_REST_URL=https://xxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxx

# ===========================================
# Celery (uses same Redis URL)
# ===========================================
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# ===========================================
# Helius API
# ===========================================
# Get from: https://helius.dev
HELIUS_API_KEY=your-helius-api-key
HELIUS_WEBHOOK_SECRET=your-webhook-secret

# ===========================================
# Solana RPC
# ===========================================
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-helius-api-key

# ===========================================
# Wallet Private Keys (Base58 encoded)
# ===========================================
# IMPORTANT: Never commit actual keys!
# Note: Buybacks use CREATOR_WALLET - no separate buyback wallet needed
CREATOR_WALLET_PRIVATE_KEY=
AIRDROP_POOL_PRIVATE_KEY=
TEAM_WALLET_PUBLIC_KEY=
ALGO_BOT_WALLET_PUBLIC_KEY=

# ===========================================
# Token Configuration (Dual-Token Model)
# ===========================================
# POH = Token users hold (determines eligibility & hash power)
POH_TOKEN_MINT=
POH_TOKEN_DECIMALS=9

# GOLD = Token distributed as rewards
GOLD_TOKEN_MINT=GoLDppdjB1vDTPSGxyMJFqdnj134yH6Prg9eqsGDiw6A
GOLD_TOKEN_DECIMALS=9

# ===========================================
# Distribution Settings
# ===========================================
DISTRIBUTION_THRESHOLD_USD=250
DISTRIBUTION_MAX_HOURS=24
MIN_BALANCE_USD=50

# ===========================================
# Reward Split (must total 100)
# ===========================================
# Configurable allocation of creator rewards
REWARD_POOL_PERCENT=80
ALGO_BOT_PERCENT=10
TEAM_PERCENT=10

# Buyback split (of the pool allocation)
# How much of pool SOL to swap to reward token vs keep as reserves
BUYBACK_SWAP_PERCENT=20
BUYBACK_RESERVE_PERCENT=80

# ===========================================
# Token Branding (for logs/display)
# ===========================================
HOLD_TOKEN_SYMBOL=POH
REWARD_TOKEN_SYMBOL=GOLD

# ===========================================
# Snapshot Settings
# ===========================================
SNAPSHOT_PROBABILITY=0.4

# ===========================================
# Monitoring (Optional)
# ===========================================
SENTRY_DSN=
